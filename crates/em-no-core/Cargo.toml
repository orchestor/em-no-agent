[package]
name = "em-no-core"
version = "0.1.0"
edition = "2021"

[dependencies]
candle-core.workspace = true
candle-nn.workspace   = true
serde = { version = "1.0", features = ["derive"] }
rand.workspace        = true